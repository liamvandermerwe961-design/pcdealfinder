let products=[];const money=n=>'R'+Number(n).toLocaleString('en-ZA');fetch('data.json').then(r=>r.json()).then(d=>{products=d;render(products.slice(0,3))});function render(list){const grid=document.getElementById('grid');grid.innerHTML='';list.forEach(p=>{const el=document.createElement('article');el.className='card';el.innerHTML=`<div class="tag">${p.category}</div><h3>${p.name}</h3><div class="mpn">${p.mpn}</div><div class="best"><small>BEST LISTED PRICE</small><strong>${money(p.price)}</strong></div><div class="listing"><div><b>${p.retailer}</b><span class="stock">● ${p.stock}</span></div><a href="${p.url}" target="_blank" rel="noopener">View deal</a></div>`;grid.appendChild(el)})}function search(q){q=(q??document.getElementById('q').value).trim().toLowerCase();render(q?products.filter(p=>(p.name+' '+p.category+' '+p.mpn).toLowerCase().includes(q)):products);document.getElementById('deals').scrollIntoView({behavior:'smooth'})}document.getElementById('q').addEventListener('keydown',e=>{if(e.key==='Enter')search()});document.querySelectorAll('[data-q]').forEach(x=>x.addEventListener('click',()=>search(x.dataset.q)));function focusSearch(){document.getElementById('q').focus();scrollTo({top:0,behavior:'smooth'})}
